module BlindfoldChessTraining.UIElems.Icons

open Fabulous.Maui
open type Fabulous.Maui.View

let empty: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/empty.png")

let home: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/home.png")

let library: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/library.png")

let annotation: Fabulous.WidgetBuilder<obj, IFabImage> =
    Image("icons/annotation.png")

let fingerprint: Fabulous.WidgetBuilder<obj, IFabImage> =
    Image("icons/fingerprint.png")

let cube: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/cube.png")

let options: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/options.png")

let speaker: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/speaker.png")

let chip: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/chip.png")

let code: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/code.png")
let idCard: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/id_card.png")

let eye: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/eye.png")

let volume_up: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/volume_up.png")

let arrowCircleLeft: Fabulous.WidgetBuilder<obj, IFabImage> =
    Image("icons/arrow_circle_left.png")

let arrowCircleRight: Fabulous.WidgetBuilder<obj, IFabImage> =
    Image("icons/arrow_circle_right.png")

let fastForward: Fabulous.WidgetBuilder<obj, IFabImage> =
    Image("icons/fast_forward.png")

let rewind: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/rewind.png")

let chevronLeft: Fabulous.WidgetBuilder<obj, IFabImage> =
    Image("icons/chevron_left.png")

let chevronRight: Fabulous.WidgetBuilder<obj, IFabImage> =
    Image("icons/chevron_right.png")

let chevronDoubleLeft: Fabulous.WidgetBuilder<obj, IFabImage> =
    Image("icons/chevron_double_left.png")

let chevronDoubleRight: Fabulous.WidgetBuilder<obj, IFabImage> =
    Image("icons/chevron_double_right.png")

let document: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/document.png")
let share: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/share.png")
let info: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/info.png")
let play: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/play.png")

let play_left: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/play_left.png")
let exit: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/exit.png")

let list: Fabulous.WidgetBuilder<obj, IFabImage> = Image("icons/list.png")
